GridGain Clients Module
-----------------------

GridGain clients module provides thin client that allows to communicate with grid topology
using REST approach without starting a GridGain node.

To enable clients module simply move 'libs/optional/gridgain-clients' folder to 'libs' folder.
