GridGain REST-TCP Module
------------------------

GridGain REST-TCP module provides binary server which can be used to execute tasks and/or cache commands
in grid using REST approach via TCP protocol.

To enable REST-TCP module simply move 'libs/optional/gridgain-rest-tcp' folder to 'libs' folder.
