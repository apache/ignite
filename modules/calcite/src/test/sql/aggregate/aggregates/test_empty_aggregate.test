# name: test/sql/aggregate/aggregates/test_empty_aggregate.test
# description: Test aggregate operators on empty set
# group: [aggregates]

statement ok
CREATE TABLE integers(ii INTEGER);

query IIRRRIII
SELECT COUNT(*), COUNT(ii), STDDEV_SAMP(ii), SUM(ii), SUM(DISTINCT ii), FIRST(ii), MAX(ii), MIN(ii) FROM integers WHERE ii > 100
----
0
0
NULL
NULL
NULL
NULL
NULL
NULL

statement ok
CREATE TABLE emptyaggr(ii INTEGER);

query I
SELECT COUNT(*) FROM emptyaggr
----
0

query RIIIRIIIII
SELECT SUM(ii), COUNT(ii), COUNT(DISTINCT ii), COUNT(*), AVG(ii), COUNT(*)+1, COUNT(ii)+1, MIN(ii), MIN(ii+1), MIN(ii)+1 FROM emptyaggr
----
NULL	0	0	0	NULL	1	1	NULL	NULL	NULL
