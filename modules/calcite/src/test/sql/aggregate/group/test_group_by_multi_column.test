# name: test/sql/aggregate/group/test_group_by_multi_column.test
# description: Group by multiple columns
# group: [group]

statement ok
CREATE TABLE integers(ii INTEGER, jj INTEGER, kk INTEGER);

statement ok
INSERT INTO integers VALUES (1, 1, 2), (1, 2, 2), (1, 1, 2), (2, 1, 2), (1, 2, 4), (1, 2, NULL);

query IIRII
SELECT ii, jj, SUM(kk), COUNT(*), COUNT(kk) FROM integers GROUP BY ii, jj ORDER BY 1, 2
----
1	1	4.000000	2	2
1	2	6.000000	3	2
2	1	2.000000	1	1

