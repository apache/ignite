# name: test/sql/types/list/list.test_slow
# description: Test big list
# group: [list]

statement ok
PRAGMA enable_verification

statement ok
CREATE TABLE test AS (SELECT range ii, 0 as jj FROM range(70000));

query I
SELECT list(ii)[69999] FROM test GROUP BY jj;
----
69999