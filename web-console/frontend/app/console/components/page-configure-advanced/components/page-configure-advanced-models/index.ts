

import angular from 'angular';
import component from './component';

export default angular
    .module('ignite-console.page-console-advanced.models', [])
    .component(component.name, component);
