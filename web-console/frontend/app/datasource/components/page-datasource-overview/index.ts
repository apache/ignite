

import angular from 'angular';

import component from './component';

export default angular
    .module('ignite-console.page-datasource-overview', [])
    .component('pageDatasourceOverview', component)
    ;
