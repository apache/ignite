

header.header-with-selector
    div
        h1 Storage Source List
    div
        a.btn-ignite.btn-ignite--primary(
            type='button'
            ui-sref='base.igfs.edit.advanced({storageID: "new"})'
        )
            svg.icon-left(ignite-icon='plus')
            | Create Storage Source Configuration
        

.pco-relative-root
    pc-items-table(
        table-title='::"Choose Storage Source"'
        column-defs='$ctrl.datasourceColumnDefs'
        items='$ctrl.dataSourceList'
        on-action='$ctrl.onDatasourceAction($event)'
        max-rows-to-show='10'
        one-way-selection='::true'
        selected-row-id='$ctrl.selectedRowsIDs$|async:this'
        on-selection-change='$ctrl.selectedRows$.next($event)'
        actions-menu='$ctrl.actions$|async:this'
    )
        footer-slot(ng-hide='$ctrl.dataSourceList.length' style='font-style: italic')
            | You have no storage source configurations.
            a.link-success(ui-sref='base.igfs.edit.advanced({storageID: "new"})') Create one?
    span 
        | {{ message }}

.pco-relative-root
    div(id='amis_root')    
    